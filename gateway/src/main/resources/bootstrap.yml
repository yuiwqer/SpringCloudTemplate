server:
  port: 1002
spring:
  application:
    name: hello
  cloud:
    gateway:
     discovery:
        locator:
         enabled: true
    nacos:
#      config:
#        file-extension: yml
#        server-addr: http://localhost:8848
      discovery:
        server-addr: http://localhost:8848