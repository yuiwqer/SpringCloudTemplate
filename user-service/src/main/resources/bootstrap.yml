server:
  port: 1001
spring:
  application:
    name: user-service
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: http://localhost:8848
      discovery:
        server-addr: http://localhost:8848